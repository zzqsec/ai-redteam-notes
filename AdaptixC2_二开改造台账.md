# AdaptixC2 v1.2 二开改造思路

> 本文**只讲思路**：为什么改、按什么原则改、怎么验证改对了。
> 不含具体文件、参数、常量与代码。
>
> 免责声明同仓库 README —— 仅限合法授权测试与安全研究。

---

## 0. 前置：先把「基准」钉死

改造对象是**多手转来的副本**，仓库里**没有版本历史**。不先做这一步，后面全是空谈。

存在两类必犯的错误：

- 把上游第三方引入的行为，当成"框架本身的设计"去分析；
- 一旦某处行为异常，**无法判断是自己改坏的、还是本来就有的**。

**做法**：先取一份干净的官方 release 作为**基线**，并**自证这份基线与官方发布逐字节一致**。
这一步是必须的 —— 如果基线本身被污染，后面所有"与上游相同"的结论全部失效。

### 比对的两个硬前提

**① 必须先做归一化。**
副本在 Windows 上过了一手，**大量文件换行符被整体改成了 CRLF**。
直接逐文件比对，会把**整个文件**算成差异 —— 看起来像"改了几百行"，实际一行没动。
⇒ 归一化（忽略换行符差异）不做，第一步就会得出错误结论。

**② 判断"改动是否真的编进去了"，要看符号 / 反汇编，不能看文件哈希。**
重生成类流程会改掉所有产物的哈希，用哈希判断会得出完全相反的结论。

---

## 1. 改造按「被谁发现」分五层

不同稽查面看到的东西不一样，所以改造也必须分层 —— **一层只解决一个问题**。

### ① 传输层 —— 被流量设备按 TLS 指纹识别

**思路**：让握手形态跟一台真实服务器**同型**。

- **反直觉点**：指纹由**握手报文里字段的排列顺序**决定，**不是**由密码套件表、协议版本决定。
  改套件、改最低版本号，对指纹**没有作用**，反而会挡掉老系统 —— 纯粹的负收益。
- **证书要稳定**：如果每次重启都重新签发，指纹每次都变。
  对指纹识别来说，**"同一台服务器每次指纹都不同"本身就是异常信号**。
- **容易忽略的落点**：证书也可能**存在数据库里**，只改磁盘上的文件，启动时会被覆盖回去。
- **构建顺序也有坑**：改动了底层标准库之后，必须**重新编译全部扩展模块**，否则模块加载不上，
  且现象是"整类功能静默消失"，不指向真正原因。

### ② 应用层 —— 默认值一查就认出

**思路**：把所有「默认」去掉。

- 默认页面、默认响应头、默认 URI、默认 UA —— 都是**可以直接拿去搜**的特征。
- 其中有一类要**删掉而不是改名**：**框架自造的、非标准的响应头**。
  真实服务器根本不会发这种头 —— 换成假名字，它**仍然是一个可被签名的非标头**。⇒ 直接删。
  删之前必须**先自证没有代码依赖它**（全库检索确认无读取方，且客户端在缺少它时功能正常）。
- **伪装页面必须纯静态、无外链、无 JS 外发** —— 否则伪装页本身就成了新的外联特征。

### ③ 植入体形态 —— 被终端杀软查杀

**这一层是重点，结论也最反直觉。**

**先说判定性质（实测得出）：**

- 判定依据是**代码形态 / 统计特征**，**不是**单条字节签名。
  证据形态是：**两个字节差异极大的样本，检出结果相同；而只差一小段代码的两个样本，检出结果相反**。
  这与"单条签名"的解释不兼容。
- 判定处在**匹配边界的边缘**：删掉一段**与功能无关**的代码，就能让检出结果**双向翻转**。
  ⇒ **「删代码 / 挪布局」躲杀软是抽奖**，不能计入防护能力。
- 特征库是**加密存储**的，静态读特征这条路走不通。

**由此推出的核心原则：**

> ### 要改「生成方式」，不是改「位置」。

- 「改位置」（重排布局、加随机前缀、随机化常量）只动**地址与常量**，
  **指令序列一字未变** ⇒ 打不到形态类判定。
- 只有**改变指令序列本身**（编译期优化、代码变形）才动得了它。
- **但反过来，"改位置"对另一类按固定偏移锚定的规则是有效的。**
  两类稽查面机制不同，**手段不能混用、也不能互相代言**。

**怎么在"特征库读不了"的前提下定位：**

转向**隔离变量 + 阳性对照**的黑盒实验法：

- 一次只变**一个**自变量（同源码只换一个目标文件 / 只换一个编译选项），看检出结果是否翻转；
- **必须带阳性对照**（同时放一个已知必被查杀的样本）。
  否则"没被删"**无法区分**是「成功逃逸」还是「扫描根本没跑」；
- **写入即扫**：文件落盘本身就是一次探针，**不必真的启动进程** ⇒ 单次实验成本很低；
- 每次触发告警都会打扰到环境 ⇒ **实验要设计成"一次拿到多个结论"**。

**另一个必须承认的事实：假对抗比不做好。**

堆砌"看起来很对抗"的代码，如果其数据**只写不读**，那就是**零收益、纯增风险** ——
不只没效果，还额外增加了可被识别的行为与静态特征。

典型形态有两种：

1. 写了一张表，但**从来没有代码查这张表** —— "能力"从未生效；
2. 往系统库里打补丁，但**当前路径根本不走那条链路** —— 白挨一次高危行为。

⇒ 改造时**优先删除**这类代码，而不是继续给它加固。

### ④ 会话密钥 —— 静态密钥能从载荷里明文读出

**思路**：让「拿到载荷 + 完整抓包」也**不足以**解密回连数据。

- 原则：**每次会话用一次性协商出的密钥**，而不是编译期烧进载荷的固定密钥。
- 关键约束：协商用的**长期私钥只留在服务端**，载荷里**只放公钥**；
  一次性私钥**用完即弃、不驻留**。
- 容易被忽略的是**平台条件编译**：不同通道走的加密路径不同，**不能无脑全局替换**。

### ⑤ 工程性 / 显示层

会话显示掉线、署名水印之类的纯显示层问题。

**原则**：**只碰显示层，不引入 socket、不写文件** ——
避免为了修一个显示问题而扩大整体行为面。

---

## 2. 验收原则

这几条都是踩过坑才定下来的：

1. **植入体侧改动的第一验收标准 = 用 C2 发一条命令并拿到回显**，**不是"能上线"**。
   曾经为了降低流量特征去改连接管理，结果 **agent 收不到命令**，只能整体回退。
   "能上线"离"能用"还很远。
2. **启动载荷要用干净的系统上下文**，否则自己的实验环境会干扰实验结果。
3. **新增源文件必须同时进入构建脚本的两处列表**（源清单 + 链接清单）。
   只加一处会 **"编译通过但链接失败"**，**报错点离真正原因很远**。
4. **改动是否生效，用符号 / 反汇编验证，不用哈希**（同 §0）。

---

## 3. 待解决

| 项 | 状态 |
|---|---|
| **流量侧（行为层）对抗** | **未解** —— 连接管理那次尝试回退后回到原点，需要新思路 |
| 加固后的 shellcode 格式载荷 | 仅完成**静态结构验证**，**未做功能验收**（需要 loader 包装才能跑起来） |

---

*本文由 AI 辅助整理、人工审核修正。技术结论均已在授权环境实测。*
